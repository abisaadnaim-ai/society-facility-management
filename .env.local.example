# Supabase project connection (society-facility-management project — see README)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only. Required for Super Admin "Add User" (Auth Admin API).
# NEVER prefix with NEXT_PUBLIC_ and never expose to the browser.
# Get it from: Supabase Dashboard → Project Settings → API → service_role secret.
SUPABASE_SERVICE_ROLE_KEY=
